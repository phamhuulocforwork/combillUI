---
title: useDebounce
description: Debounce a value to avoid excessive renders.
preview: true
---

<ComponentTabs name='use-debounce-default' />

## Installation

<ShadcnTabs defaultValue='cli'>
  <TabsList >
    <TabsTrigger  value='cli'>
      CLI
    </TabsTrigger>
    <TabsTrigger  value='manual'>
      Manual
    </TabsTrigger>
  </TabsList>

  <TabsContent  value='cli'>
    <Tabs items={["npm", "pnpm", "yarn", "bun"]}>
      <Tab value="npm">
        ```bash
        npx shadcn@latest add 'https://combillui.vercel.app/registry/use-debounce.json';
        ```
      </Tab>
      
      <Tab value="pnpm">
        ```bash 
        pnpm dlx shadcn@latest add 'https://combillui.vercel.app/registry/use-debounce.json';
        ```
      </Tab>

      <Tab value="yarn">
        ```bash
        npx dlx shadcn@latest add 'https://combillui.vercel.app/registry/use-debounce.json';
        ```
      </Tab>

      <Tab value="bun">
        ```bash
        bunx --bun shadcn@latest add 'https://combillui.vercel.app/registry/use-debounce.json';
        ```
      </Tab>
    </Tabs>

  </TabsContent>
  <TabsContent  value='manual'>
    Copy and paste the following code into your project.
    <ComponentSource name="use-debounce" />
  </TabsContent>
</ShadcnTabs>

## Usage

```tsx
"use client";

import { useDebounce } from "@/hooks/use-debounce";
```

```tsx
const [searchTerm, setSearchTerm] = useState("");
const debouncedSearchTerm = useDebounce(searchTerm, 500);
```

The hook:

- Takes any value and an optional delay (default: 500ms)
- Returns a debounced version of the value
- Useful for search inputs, form validation, and API calls
