---
title: Kbd
description: A keyboard key primitive for displaying keyboard shortcuts and key combinations.
preview: true
links:
  api: /docs/components/kbd#api-reference
---

<ComponentTabs name='kbd-default' />

## Installation

<ShadcnTabs defaultValue='cli'>
  <TabsList >
    <TabsTrigger  value='cli'>
      CLI
    </TabsTrigger>
    <TabsTrigger  value='manual'>
      Manual
    </TabsTrigger>
  </TabsList>

  <TabsContent  value='cli'>
    <Tabs items={["npm", "pnpm", "yarn", "bun"]}>
      <Tab value="npm">
        ```bash
        npx shadcn@latest add 'https://combillui.vercel.app/registry/kbd.json';
        ```
      </Tab>
      
      <Tab value="pnpm">
        ```bash 
        pnpm dlx shadcn@latest add 'https://combillui.vercel.app/registry/kbd.json';
        ```
      </Tab>

      <Tab value="yarn">
        ```bash
        npx dlx shadcn@latest add 'https://combillui.vercel.app/registry/kbd.json';
        ```
      </Tab>

      <Tab value="bun">
        ```bash
        bunx --bun shadcn@latest add 'https://combillui.vercel.app/registry/kbd.json';
        ```
      </Tab>
    </Tabs>

  </TabsContent>
  <TabsContent  value='manual'>
    <Steps>
      <Step>
        Install the following dependencies:

        ```package-install
          @radix-ui/react-slot
        ```
      </Step>
      <Step>
        Copy and paste the following code into your project.
        <ComponentSource name="kbd" />
      </Step>
    </Steps>

  </TabsContent>
</ShadcnTabs>

## Usage

Import the parts, and compose them together.

```tsx
import * as Kbd from "@/components/ui/kbd";

<Kbd.Root>
  <Kbd.Key />
  <Kbd.Separator />
  <Kbd.Key />
</Kbd.Root>;
```

### Built-in Key Titles

The component includes built-in titles for common keyboard symbols:

```tsx
<Kbd.Root>
  <Kbd.Key>⌘</Kbd.Key> {/* Shows "Command" on hover */}
  <Kbd.Key>⇧</Kbd.Key> {/* Shows "Shift" on hover */}
  <Kbd.Key>⌥</Kbd.Key> {/* Shows "Option" on hover */}
</Kbd.Root>
```

### Custom Key Titles

You can provide custom titles for any key:

```tsx
<Kbd.Root>
  <Kbd.Key title='Windows key'>⊞</Kbd.Key>
  <Kbd.Separator />
  <Kbd.Key title='Lock screen'>L</Kbd.Key>
</Kbd.Root>
```

## Examples

### With Multiple Keys

<ComponentTabs name='kbd-multiple' />

### With Variants

<ComponentTabs name='kbd-variants' />

## API Reference

### Root

The main container component for keyboard shortcuts.

<AutoTypeTable path='./types/docs/kbd.ts' name='RootProps' />

<DataAttributesTable
  attributes={[
    {
      title: "[data-slot]",
      value: ["kbd"],
    },
  ]}
/>

### Key

The component that represents a single keyboard key.

<AutoTypeTable path='./types/docs/kbd.ts' name='KeyProps' />

<DataAttributesTable
  attributes={[
    {
      title: "[data-slot]",
      value: ["kbd-key"],
    },
  ]}
/>

### Separator

The component that represents the separator between keyboard keys.

<AutoTypeTable path='./types/docs/kbd.ts' name='SeparatorProps' />

<DataAttributesTable
  attributes={[
    {
      title: "[data-slot]",
      value: ["kbd-separator"],
    },
  ]}
/>
